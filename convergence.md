# CCM Convergence 判定 Design Doc

## 1. 背景 / 目的

Empirical Dynamic Modeling (EDM) の Convergent Cross Mapping (CCM) では、**library size (L)** を増やすと cross map skill が改善し、やがて **飽和（収束）**する、という挙動を期待する。  
研究・解析パイプライン上で、CCM の「収束っぽい形状」を**目視ではなく機械的・定量的に判定**したい。

本 design doc は、AI Agent が実装可能な **計算量が小さく、パラメータが多くない**収束判定アルゴリズムを定義する。

---

## 2. 対象データ / 前提

### 2.1 入力
- library size の系列:  
  - `L = [L1, L2, ..., Ln]`（n = 5〜10 程度）
  - 基本は等間隔だが、対数間隔でもよい（アルゴリズムはどちらにも対応）
- cross map skill の系列（L ごとに複数サンプル）:
  - Pearson correlation `r`（大きいほど良い）
  - MAE / RMSE（小さいほど良い）
- 各 L に対してサンプリング（ブートストラップ/サブサンプル等）を `m = 20〜100` 回実施し、skill を得る  
  `S[L_i] = { s_{i,1}, ..., s_{i,m} }`

### 2.2 重要な要件
- 収束判定の主目的は **「全体として飽和（収束）形状か」**の判定
- n が小さい（5〜10）ため、時間系列的な滑らかさや多点回帰に依存しない
- パラメータ数を少なくし、チューニング負荷を抑える

---

## 2.5 現状の実装と課題

現在の `ccm_convergence_test` は以下のように動作する：

- 各 `lib_size` で `num_samples` 回サブサンプリングし、`np.corrcoef` で rho を計算して平均
- 収束スコア = `rho[-1] - rho[0]`（最大 lib_size と最小 lib_size の差）
- `mde()` 内で `convergence_score >= ccm_threshold`（デフォルト 0.1）で合否判定

**問題点：**

1. 単純差分ではノイズの影響を受けやすく、収束形状（飽和カーブ）を検出できない
2. rho をハードコードしており、MAE / RMSE 等の metric に非対応
3. サンプル間のばらつき（分散）を考慮していない

---

## 3. 収束判定の基本方針

「収束っぽい形状」を以下の 2 観点で捉える：

1. **全体形状**: 飽和モデル（収束）でよく説明できるか  
2. **末尾挙動**: 末尾の改善が統計的にほぼ 0（頭打ち）か

最終判定は **(1) と (2) を両方満たす**ことを推奨（頑健）。

---

## 4. スコア表現の統一

すべての指標を「大きいほど良い」スコア `s(L)` に統一する。

- Pearson correlation:
  - `s = r`
- MAE / RMSE:
  - `s = -error`（符号反転）

以降、`s` を用いて判定する。

---

## 5. 判定手法 A：飽和モデル vs 非飽和モデル（AICc）

### 5.1 モデル定義（低パラメータ）
**飽和（収束）モデル（3パラメータ）**：指数飽和
\[
s(L) = a - b e^{-cL}, \quad b>0,\; c>0
\]

**非飽和モデル（2パラメータ）**：線形
\[
s(L) = \alpha + \beta L
\]

> 注: L が対数間隔の場合もそのまま適用可能。  
> 代替案として `s(L)=α+β log L` を非飽和側に置くこともできるが、本設計では最小構成として線形を採用。

### 5.2 重み付き最小二乗（WLS）
各 L に対してサンプル平均と分散を計算：
- `mu_i = mean(S[L_i])`
- `var_i = var(S[L_i])`（不偏分散推奨）
- 重み `w_i = 1 / max(var_i, eps_var)`（ゼロ割防止の下限 `eps_var` を設ける）

目的関数（モデル f の場合）：
\[
\text{SSE}_w = \sum_i w_i (mu_i - f(L_i))^2
\]

### 5.3 AICc によるモデル比較
観測点は `n`（= L の個数）。推定パラメータ数を `k` とする（飽和: 3、線形: 2）。

- まず残差分散の代替として `RSS = Σ (mu_i - f(L_i))^2` を用いる（実装容易性優先）
- AIC:
\[
\text{AIC} = n \ln(RSS/n) + 2k
\]
- AICc（小標本補正）:
\[
\text{AICc} = \text{AIC} + \frac{2k(k+1)}{n-k-1}
\]

比較量：
- `ΔAICc = AICc_linear - AICc_saturation`

**判定基準（推奨）**
- `ΔAICc >= 4` : 飽和モデルが有利（収束形状を支持）
- `ΔAICc >= 10`: 強い支持

> 注意: n が小さいため、`n <= k+1` の場合 AICc が定義できない。  
> その場合は AIC のみ、または BIC を用いる fallback を実装する。

### 5.4 飽和モデルの制約（推奨）
- `b>0, c>0`（単調増加＋上限への接近）
- 実装は `b=exp(b_raw)`、`c=exp(c_raw)` のように再パラメータ化すると簡単。

### 5.5 初期値（推奨）
フィットを安定させるため、初期値を以下で与える：
- `a0 = max(mu_i)`（最終値近辺の上限）
- `b0 = a0 - mu_1`（初期と上限の差）
- `c0` は「半分まで到達する L」から粗く推定（なければ小さめ定数で開始）

---

## 6. 判定手法 B：末尾改善がゼロ（ブートストラップ）

### 6.1 末尾差分 Δ の推定
- `L_max = L_n`
- `L_2nd = L_{n-1}`（末尾 2 点のみ使用）
- 末尾改善量:
\[
\Delta = s(L_{max}) - s(L_{2nd})
\]

### 6.2 ブートストラップ手順（低コスト）
各 L のサンプル集合 `S[L]` から、同サイズで復元抽出し平均を取る：

1. `B` 回繰り返し（例: B=1000）
2. `S*[L_max]` と `S*[L_2nd]` を resample
3. `Δ_b = mean(S*[L_max]) - mean(S*[L_2nd])`
4. `Δ` の分布から 95% CI（2.5%, 97.5%）

**判定基準（推奨）**
- `CI(Δ)` が 0 を跨ぐ（=末尾改善が統計的にゼロっぽい）なら「頭打ち」

### 6.3 スケール不変な末尾改善比 r
全体変化に対する末尾変化の比:
\[
r = \frac{\Delta}{\bar{s}(L_{max}) - \bar{s}(L_{min})}
\]
ここで `bar{s}` はサンプル平均。分母が 0 に近い場合は扱いに注意（下限 eps を設ける）。

**判定基準（推奨）**
- `median(r_boot) < 0.05`（末尾の伸びが全体伸びの 5% 未満）

---

## 7. 最終判定ロジック（推奨）

入力から `mu_i, var_i` を計算し、以下を評価する：

1. **形状判定**（AICc）  
   - `ΔAICc >= 4`（or 10 for strong）
2. **末尾判定**（ブートストラップ）  
   - `CI(Δ)` が 0 を跨ぐ  
   - かつ `median(r_boot) < 0.05`（任意だが推奨）

**最終結論**  
- `converged = (1) AND (2)`  
- 研究報告用に「形状のみ」「末尾のみ」も個別に返せるようにする

---

## 8. 出力仕様（例）

```json
{
  "converged": true,
  "score_transform": "s = r or -error",
  "n_L": 8,
  "m_samples_per_L": 50,
  "model_comparison": {
    "aicc_saturation": -12.3,
    "aicc_linear": -5.1,
    "delta_aicc": 7.2,
    "threshold": 4
  },
  "tail_test": {
    "delta_mean": 0.0012,
    "delta_ci95": [-0.0021, 0.0034],
    "r_median": 0.021,
    "r_ci95": [0.0, 0.08],
    "r_threshold": 0.05
  },
  "notes": [
    "ΔAICc >= 4 supports saturating shape",
    "Tail improvement CI overlaps 0"
  ]
}
```

---

## 9. 計算量 / 実装上の注意

- 形状判定：非線形最小二乗（飽和モデル）＋線形回帰  
  - n が 5〜10 と小さいので計算量は非常に小さい
- ブートストラップ：`O(B * m)`（B=1000, m<=100 なら軽量）
- 数値安定性：
  - `eps_var` や分母の `eps` を設ける（例: 1e-12）
  - 飽和モデルの制約は exp 再パラメータ化で安全に実装

---

## 10. エッジケース / フォールバック

- `n <= 4` などで AICc が不安定:
  - AIC のみで比較、または形状判定を「参考値」として扱う
- 全体変化がほぼゼロ（分母が小さい）:
  - `r` は未定義に近い → 末尾改善比の判定をスキップし、CI(Δ) のみに依存
- skill が単調でない（ノイズで一時的に悪化）:
  - 本設計は単調性を直接検定しない（制約付き飽和モデル＋重み付けで吸収）
  - 必要なら補助指標として Kendall τ を算出しログ出力する

---

## 11. 推奨デフォルトパラメータ

- `B = 1000`（ブートストラップ反復）
- `ΔAICc_threshold = 4`（strong は 10）
- `r_threshold = 0.05`
- `eps_var = 1e-12`

---

## 12. 最低限の実装タスク

1. 入力整形（r / MAE / RMSE → スコア s へ統一）
2. `mu_i, var_i, w_i` の計算
3. 線形モデル（OLS/WLS）フィットと AICc
4. 飽和モデル（制約付き）フィットと AICc
5. ブートストラップで `Δ` と `r` の分布、CI/中央値の算出
6. 最終判定と JSON 形式のレポート出力
