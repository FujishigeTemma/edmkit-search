# テクニカルライティングガイド

本ドキュメントは、ドキュメントを執筆する際に従うべき規約とベストプラクティスをまとめたものである。AI Agentがユーザーの書いた資料をレビューする際の判断基準として使用する。

## レビュー前の前提確認

レビューを開始する前に、まずユーザーとの対話を通じて以下の項目を明確にする。これらが不明なままでは、「書かれるべきなのに書かれていない情報」を指摘できない。

- **目的** — このドキュメントは何を達成するために書かれたのか（例: 手順の説明、仕様の共有、トラブルシューティング）
- **対象読者（ペルソナ）** — 誰が読むのか。技術レベル、前提知識、役割（例: 新規参画の開発者、運用担当者、外部パートナー）
- **カバー範囲** — このドキュメントが扱うべきトピックの範囲と、明示的に扱わないトピック
- **利用シーン** — 読み手はどのような状況でこのドキュメントを参照するのか（例: 初回セットアップ時、障害発生時、日常の開発作業中）
- **既存の規約・用語集** — プロジェクト固有のスタイルガイドや用語集があるか。ある場合はその内容を把握した上でレビューする

これらの情報がユーザーから得られた上で、以下の規約に基づいてレビューを行う。

## 目次

- [レビュー前の前提確認](#レビュー前の前提確認)
- [基本原則](#基本原則)
- [1文の書き方](#1文の書き方)
- [トピックの構成](#トピックの構成)
- [複数トピックの整理](#複数トピックの整理)
- [用語と表記の一貫性](#用語と表記の一貫性)
- [コードと技術要素の記述](#コードと技術要素の記述)
- [アクセシビリティと包括性](#アクセシビリティと包括性)
- [セルフレビューチェックリスト](#セルフレビューチェックリスト)
- [執筆プロセス](#執筆プロセス)
- [参考資料](#参考資料)

---

## 基本原則

テクニカルライティングでは、次の3つの原則（3C）を常に意識する。

### 明確性（Clarity）

読み手が「読み方を工夫」しなくても、淡々と読むだけで意味が伝わる文章を書く。曖昧さを排除し、誰が何をするのかを明示する。

### 簡潔性（Conciseness）

文は短く保つ。不要な修飾語や冗長な表現を削り、伝えるべき情報だけを残す。

### 一貫性（Consistency）

同じ概念には同じ用語を使い続ける。表記ゆれはドキュメント全体の信頼性を損なう。

---

## 1文の書き方

わかりやすい文章は、最初から生まれるものではない。複数の文案を書き、最も伝わりやすいものを選ぶことが重要である。

### 主語と述語を明確にする

各述語に対して主語が明らかであることを確認する。主語と述語の距離は近く保つ。

**悪い例:**

> ユーザーがログインすると、プロフィールを取得できます。

この文では、誰がプロフィールを取得するのかが不明瞭である。ユーザーが取得するのか、アプリが取得するのかがわからない。

**良い例:**

> ユーザーがログインすると、アプリはユーザーのプロフィールを取得する。

### 1文を短く保つ

1文には1つの事柄だけを書く。「〜ので」「〜し」「〜が」で接続された長い文は、句点で分割する。

**悪い例:**

> この関数はユーザーの入力を検証し、不正な値が含まれている場合はエラーを返すので、呼び出し側ではエラーハンドリングが必要です。

**良い例:**

> この関数はユーザーの入力を検証する。不正な値が含まれている場合はエラーを返す。呼び出し側ではエラーハンドリングが必要である。

### 語順に配慮する

語順によって意味が変わる場合がある。意図した意味が正確に伝わる語順を選ぶ。

**例:**

- 「いろいろな漢字の読み方」→ 読み方がいろいろあるように読める
- 「漢字のいろいろな読み方」→ 漢字の読み方がいろいろあることが明確

### 能動態を使う

受動態は動作の主体を曖昧にする。誰が何をするのかを明確に伝えるため、能動態を基本とする。

**悪い例:**

> 設定ファイルが読み込まれ、パラメータが適用されます。

**良い例:**

> アプリケーションは起動時に設定ファイルを読み込み、パラメータを適用する。

### 代名詞を明確にする

「これ」「それ」「あれ」が何を指すのかが曖昧な場合は、具体的な名詞に置き換える。

**悪い例:**

> APIキーを環境変数に設定する。これを使って認証を行う。

**良い例:**

> APIキーを環境変数に設定する。アプリケーションはこのAPIキーを使って認証を行う。

### 箇条書きと表を活用する

複数の項目を列挙する場合は、箇条書きや表を使って情報を整理する。ただし、すべてを箇条書きにするのではなく、地の文と比較して読みやすい方を選ぶ。

**箇条書きの使い分け:**

- 順序のない項目の列挙 → 箇条書き（ビュレット）
- 順序のある手順 → 番号付きリスト
- 複数の属性を持つ項目の比較 → 表

### 前提知識に注意する

読み手にとって未知の用語や概念が含まれる場合は、次のいずれかで対処する。

- 用語の初出時に簡潔な定義を添える
- 詳細な説明へのリンクを貼る
- 前提知識を「前提条件」セクションに明記する

ただし、対象読者にとって既知の事柄を過度に説明しない。読者像を明確にし、その読み手に合った粒度で書く。

---

## トピックの構成

1つのトピック（ページ、セクション）は、次の3つの要素で構成する。

### タイトル

タイトルの目的は、読み手を文章の内容に近づけることである。次の点を意識する。

- 問題点や結論をタイトルに含める
- 話題を限定する
- 読み手が「読む価値があるか」を判断できるようにする

**悪い例:**

> 認証について

**良い例:**

> OAuth 2.0を使ったAPI認証の実装手順

### 概要（リード文）

タイトルだけでは伝えきれない情報を補足し、話題をさらに限定する。概要を読めば、本文を読むべきかどうかを判断できるようにする。

「読む価値がない」ことを正しく伝えることも重要である。不要な情報を読ませないことが、ドキュメント全体の理解度向上につながる。

### 本文

本文の情報は、以下のいずれかの順序で配置する。思いついた順、書きやすい順、伝えたい順で並べてはならない。

| 順序 | 適用場面 | 説明 |
| --- | --- | --- |
| 時系列 | 操作手順、チュートリアル | 時間の経過に沿って並べる。手順書で最もよく使う |
| 粒度 | 概念の説明、リファレンス | 大まかな情報から詳細へ段階的に掘り下げる |
| 重要度 | 議事録、トラブルシューティング | 重要な項目から順に並べる |

#### 本文の論理的な流れ

基礎から応用へ、自然な学習パスを反映させる。各セクションは、前のセクションの内容を前提として構築する。

具体的には、次の順序で情報を提示する。

1. **What（何か）** — 対象の定義や概要
2. **Why（なぜか）** — その技術や機能が必要な理由
3. **How（どのように）** — 具体的な使い方や実装方法

---

## 複数トピックの整理

ドキュメント全体で複数のトピックを扱う場合は、次の観点で整理する。

### 読者の立場で取捨選択する

すべての読み手がすべてのトピックを必要とするわけではない。管理者向けと利用者向けなど、読み手ごとに不要な情報を分離する。

### 読むタイミングで分類する

トピックを次の3つのカテゴリに分類し、構成に反映する。

| カテゴリ | 内容 | 例 |
| --- | --- | --- |
| 初回のみ | セットアップ時だけ必要な情報 | インストール手順、初期設定 |
| 日常的 | 日々の作業で参照する情報 | APIリファレンス、操作ガイド |
| まれ | 特殊な状況でのみ必要な情報 | トラブルシューティング、移行ガイド |

### 区別できるタイトルを付ける

各トピックのタイトルは、他のトピックと比較して内容を容易に区別できるものにする。読み手が「自分に必要か」を即座に判断できるタイトルを付ける。

**悪い例:**

- 設定（その1）
- 設定（その2）

**良い例:**

- データベース接続の設定
- ログ出力の設定

---

## 用語と表記の一貫性

### 用語の統一

同じ概念を指す場合は、ドキュメント全体で同じ用語を使い続ける。たとえば「ユーザーエージェント」と「ブラウザ」を同じ意味で混在させない。

用語の揺れが発生しやすい例：

| 統一する | 避ける |
| --- | --- |
| サーバー | サーバ |
| ユーザー | ユーザ |
| ディレクトリ | フォルダ（文脈による） |

プロジェクトで用語集を定めている場合は、それに従う。

### 文体の統一

1つのドキュメント内で文体を混在させない。

- 「である」調と「ですます」調を混ぜない
- 体言止めと用言止めを不規則に混ぜない

### 表記の統一

- 英数字は半角を使用する
- 全角文字と半角英数字の間にスペースを入れるかどうか、プロジェクトのルールに従う
- 括弧の種類（丸括弧、角括弧など）の使い分けを統一する

---

## コードと技術要素の記述

### インラインコード

本文中でコード要素（関数名、変数名、コマンド、ファイル名など）に言及する場合は、バッククォートで囲む。

> `getUserProfile` 関数は、ユーザーIDを引数に取り、プロフィールオブジェクトを返す。

### コードブロック

複数行のコードには、言語を指定したフェンスドコードブロックを使用する。

````markdown
```javascript
const user = await getUserProfile(userId);
console.log(user.name);
```
````

コードブロックに含めるコードは、次の条件を満たすようにする。

- コピー&ペーストで動作する
- 必要な前提条件（インポート文など）を省略しない
- プレースホルダーを使う場合は、形式を統一し、置き換えが必要であることを明示する（例: `YOUR_API_KEY`）

### コマンドの記述

ターミナルで実行するコマンドには、プロンプト記号（`$`）を含めない。読み手がコピー&ペーストする際の妨げになる。

**悪い例:**

```bash
$ npm install
$ npm run build
```

**良い例:**

```bash
npm install
npm run build
```

---

## アクセシビリティと包括性

### 画像の代替テキスト

すべての画像に代替テキスト（alt属性）を提供する。スクリーンリーダーを使用する読み手がいることを想定する。

```markdown
![ログイン画面のスクリーンショット。ユーザー名とパスワードの入力欄、ログインボタンが表示されている](./images/login-screen.png)
```

### リンクテキスト

リンクのテキストは、リンク先の内容がわかるものにする。「こちら」「ここ」などの曖昧なテキストは使わない。

**悪い例:**

> 詳しくは[こちら](https://example.com/guide)を参照。

**良い例:**

> 詳しくは[認証ガイド](https://example.com/guide)を参照する。

### 包括的な表現

- 特定のジェンダーを想定した表現を避ける
- 技術的に不正確でない限り、平易な表現を選ぶ
- 非ネイティブスピーカーを意識し、慣用表現やスラングを避ける

---

## セルフレビューチェックリスト

ドキュメントを公開する前に、以下のチェックリストで確認する。

### 文レベル

- [ ] 各文の主語と述語は明確か
- [ ] 1文に複数の事柄を詰め込んでいないか
- [ ] 能動態を基本としているか
- [ ] 代名詞の指示対象は明確か
- [ ] 読み手が前提知識なしで理解できるか、または前提知識を明記しているか

### 構成レベル

- [ ] タイトルは具体的で、話題を限定しているか
- [ ] 概要（リード文）で本文の内容を正しく要約しているか
- [ ] 情報の並び順は適切か（時系列/粒度/重要度）
- [ ] What → Why → How の流れになっているか
- [ ] 各セクションに明確な目的があるか

### ドキュメント全体

- [ ] 用語は統一されているか
- [ ] 文体は統一されているか
- [ ] 表記（半角/全角、スペースなど）は統一されているか
- [ ] コード例はコピー&ペーストで動作するか
- [ ] リンクは有効か
- [ ] 画像に代替テキストがあるか
- [ ] リンクテキストはリンク先の内容を表しているか

### 最終確認

- [ ] 声に出して読み、流れと明確性を確認したか
- [ ] 冗長な表現を削除したか
- [ ] 時間を置いて再度レビューしたか

---

## 執筆プロセス

効率的に質の高いドキュメントを書くために、次のプロセスを推奨する。

1. **下書き** — 構成を気にせず、伝えたい内容を書き出す
2. **構成の整理** — 情報の順序を並べ替え、適切な見出しを付ける
3. **推敲** — 1文ずつ見直し、短く明確な文に整える
4. **つなぎの改善** — セクション間の論理的なつながりを確認し、必要に応じて情報を追加・削除する
5. **セルフレビュー** — チェックリストに沿って全体を確認する
6. **休憩後の再レビュー** — 時間を置き、新鮮な目で再度確認する

---

## 参考資料

- [LINEのテクニカルライティング講座（第1回）](https://engineering.linecorp.com/ja/blog/line-technical-writing-course) — 1文のわかりやすさ
- [LINEのテクニカルライティング講座（第2回）](https://engineering.linecorp.com/ja/blog/line-technical-writing-course-2) — トピックの構成
- [LINEのテクニカルライティング講座（第3回）](https://engineering.linecorp.com/ja/blog/line-technical-writing-course-3) — 複数トピックの整理
- [MDN Technical Writing](https://developer.mozilla.org/en-US/blog/technical-writing/) — 3Cの原則
- 一般財団法人テクニカルコミュニケーター協会「日本語スタイルガイド（第3版）」
