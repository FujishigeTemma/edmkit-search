[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "multi-target-mde"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "edmkit==0.0.0a1",
    "line-profiler>=5.0.0",
    "marimo>=0.19.6",
    "matplotlib>=3.10.8",
    "polars>=1.37.1",
    "pyzmq>=27.1.0",
    "scipy>=1.15.1",
]

[dependency-groups]
dev = ["ruff>=0.14.14", "ty>=0.0.13"]

